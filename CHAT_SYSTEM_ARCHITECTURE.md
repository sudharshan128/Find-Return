# 📐 Chat System Architecture Diagram

## System Architecture Overview

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER LAYER                               │
│  ┌──────────────┐              ┌──────────────┐                 │
│  │   Finder     │              │   Claimant   │                 │
│  │  (Browser)   │              │  (Browser)   │                 │
│  └──────┬───────┘              └──────┬───────┘                 │
│         │                             │                          │
│         │ JWT Token                   │ JWT Token                │
│         ▼                             ▼                          │
└─────────────────────────────────────────────────────────────────┘
          │                             │
          └─────────────┬───────────────┘
                        │
                        │ HTTPS
                        ▼
┌─────────────────────────────────────────────────────────────────┐
│                    FRONTEND LAYER (React)                        │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │ ChatsListPage│  │  ChatPage    │  │AdminControls │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                  │                   │
│         └─────────────────┼──────────────────┘                   │
│                           │                                      │
│                           ▼                                      │
│                  ┌────────────────┐                              │
│                  │ Supabase Client│                              │
│                  │  (Auth + DB)   │                              │
│                  └────────┬───────┘                              │
└───────────────────────────┼──────────────────────────────────────┘
                            │
              ┌─────────────┼─────────────┐
              │             │             │
              ▼             ▼             ▼
    ┌─────────────┐ ┌──────────────┐ ┌──────────────┐
    │  REST API   │ │  Realtime    │ │   Auth API   │
    │  Requests   │ │  WebSocket   │ │   (Supabase) │
    └─────┬───────┘ └──────┬───────┘ └──────┬───────┘
          │                │                │
          │                │                │
          └────────────────┼────────────────┘
                           │
                           ▼
┌─────────────────────────────────────────────────────────────────┐
│                   BACKEND LAYER (Node.js)                        │
│  ┌──────────────────────────────────────────────────────┐       │
│  │                  Express Server                       │       │
│  │  ┌──────────────────────────────────────────────┐    │       │
│  │  │            Auth Middleware                    │    │       │
│  │  │  • JWT Validation                             │    │       │
│  │  │  • User Active Check                          │    │       │
│  │  └──────────────────┬────────────────────────────┘    │       │
│  │                     ▼                                  │       │
│  │  ┌──────────────────────────────────────────────┐    │       │
│  │  │          Rate Limiting Layer                  │    │       │
│  │  │  • 50 messages/minute per user                │    │       │
│  │  │  • In-memory tracking                         │    │       │
│  │  └──────────────────┬────────────────────────────┘    │       │
│  │                     ▼                                  │       │
│  │  ┌──────────────────────────────────────────────┐    │       │
│  │  │            Chat Routes Handler                │    │       │
│  │  │  • GET    /api/chats                          │    │       │
│  │  │  • GET    /api/chats/:id                      │    │       │
│  │  │  • POST   /api/chats/:id/messages             │    │       │
│  │  │  • PUT    /api/chats/:id/read                 │    │       │
│  │  │  • PUT    /api/chats/:id/freeze (admin)       │    │       │
│  │  │  • DELETE /api/chats/:id/messages/:id (admin) │    │       │
│  │  └──────────────────┬────────────────────────────┘    │       │
│  └───────────────────────┼──────────────────────────────────┘       │
└────────────────────────────┼──────────────────────────────────────┘
                             │
                             │ Supabase JS Client
                             │ (with user JWT)
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                 DATABASE LAYER (PostgreSQL)                      │
│  ┌──────────────────────────────────────────────────────┐       │
│  │               Supabase (PostgreSQL)                   │       │
│  │  ┌──────────────────────────────────────────────┐    │       │
│  │  │          Row Level Security (RLS)             │    │       │
│  │  │  • Check auth.uid() = participant             │    │       │
│  │  │  • Verify claim approved                      │    │       │
│  │  │  • Check chat not frozen                      │    │       │
│  │  └──────────────────┬────────────────────────────┘    │       │
│  │                     ▼                                  │       │
│  │  ┌─────────────┬──────────────┬──────────────┐        │       │
│  │  │    chats    │   messages   │  audit_logs  │        │       │
│  │  ├─────────────┼──────────────┼──────────────┤        │       │
│  │  │ • id        │ • id         │ • id         │        │       │
│  │  │ • item_id   │ • chat_id    │ • user_id    │        │       │
│  │  │ • claim_id  │ • sender_id  │ • action     │        │       │
│  │  │ • finder_id │ • message_   │ • resource_  │        │       │
│  │  │ • claimant_ │   text       │   type       │        │       │
│  │  │   id        │ • is_read    │ • resource_  │        │       │
│  │  │ • enabled   │ • is_deleted │   id         │        │       │
│  │  │ • is_closed │ • created_at │ • details    │        │       │
│  │  │ • is_frozen │              │ • created_at │        │       │
│  │  │ • unread_   │              │              │        │       │
│  │  │   counts    │              │              │        │       │
│  │  └─────────────┴──────────────┴──────────────┘        │       │
│  │                     ▲                                  │       │
│  │  ┌──────────────────┴────────────────────────────┐    │       │
│  │  │               Triggers                         │    │       │
│  │  │  • update_chat_on_message                     │    │       │
│  │  │    (increment unread count)                   │    │       │
│  │  │  • reset_unread_count                         │    │       │
│  │  │    (on message read)                          │    │       │
│  │  └───────────────────────────────────────────────┘    │       │
│  └──────────────────────────────────────────────────────────┘       │
└─────────────────────────────────────────────────────────────────┘
```

---

## Data Flow Diagrams

### 1. Send Message Flow

```
User Types Message
      │
      ▼
Frontend Validation
│ • Length check (<=2000)
│ • Not empty
│ • Chat status check
      │
      ▼
POST /api/chats/:id/messages
      │
      ▼
Backend Auth Middleware
│ • Validate JWT
│ • Check user active
      │
      ▼
Rate Limiting
│ • Check count < 50/min
│ • Increment counter
      │
      ▼
Backend Validation
│ • Message not empty
│ • Length <= 2000
│ • Chat exists
│ • User is participant
│ • Chat not frozen/closed
      │
      ▼
Database Insert
      │
      ▼
RLS Policy Check
│ • sender_id = auth.uid()
│ • User is participant
│ • Chat enabled/not frozen
      │
      ▼
Insert Success
      │
      ├──────────────┬──────────────┐
      ▼              ▼              ▼
   Message      Trigger Fires    Realtime
   Returned     (increment      Broadcast
                unread count)
      │              │              │
      └──────────────┴──────────────┘
                     │
                     ▼
              Frontend Receives
                     │
                     ▼
              Message Appears in UI
```

### 2. Real-time Message Delivery Flow

```
User A Sends Message
      │
      ▼
Message Inserted in DB
      │
      ▼
Supabase Realtime Detects Change
      │
      ├──────────────┬──────────────┐
      ▼              ▼              ▼
  Check RLS      Check RLS      Check RLS
  for User A     for User B     for User C
  (sender)       (recipient)    (unrelated)
      │              │              │
      ▼              ▼              ▼
   ALLOW          ALLOW           DENY
      │              │              │
      ▼              ▼              ▼
  WebSocket      WebSocket      No message
  to User A      to User B         sent
      │              │
      ▼              ▼
  Frontend       Frontend
  receives       receives
      │              │
      ▼              ▼
  Shows in       Shows in
   chat UI        chat UI
  (immediate)    (real-time)
```

### 3. Admin Freeze Chat Flow

```
Admin Clicks "Freeze Chat"
      │
      ▼
Enter Freeze Reason
      │
      ▼
PUT /api/chats/:id/freeze
      │
      ▼
Backend Auth Middleware
│ • Validate JWT
│ • Check admin role
      │
      ▼
Update Chat Record
│ • is_frozen = true
│ • frozen_at = now
│ • frozen_by = admin_id
│ • freeze_reason = reason
      │
      ├──────────────┬──────────────┐
      ▼              ▼              ▼
  Audit Log      Chat Updated   Realtime
  Created                       Broadcast
      │              │              │
      └──────────────┴──────────────┘
                     │
                     ▼
        Both Users See "Frozen" Status
                     │
                     ▼
        Cannot Send New Messages
```

---

## Security Layers Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                        ATTACK SURFACE                            │
│                     (Public Internet)                            │
└───────────────────────────┬─────────────────────────────────────┘
                            │
                            ▼
         ┌──────────────────────────────────┐
         │   Layer 1: HTTPS/TLS             │
         │   • Encryption in transit        │
         │   • Certificate validation       │
         └──────────────┬───────────────────┘
                        ▼
         ┌──────────────────────────────────┐
         │   Layer 2: JWT Authentication    │
         │   • Token validation             │
         │   • Expiry check                 │
         │   • Signature verification       │
         └──────────────┬───────────────────┘
                        ▼
         ┌──────────────────────────────────┐
         │   Layer 3: Rate Limiting         │
         │   • 50 messages/minute           │
         │   • Per-user tracking            │
         │   • DoS prevention               │
         └──────────────┬───────────────────┘
                        ▼
         ┌──────────────────────────────────┐
         │   Layer 4: Backend Validation    │
         │   • Input sanitization           │
         │   • Business logic checks        │
         │   • Authorization verification   │
         └──────────────┬───────────────────┘
                        ▼
         ┌──────────────────────────────────┐
         │   Layer 5: Database RLS          │
         │   • Row-level security           │
         │   • Participant verification     │
         │   • Status checks (frozen/closed)│
         └──────────────┬───────────────────┘
                        ▼
         ┌──────────────────────────────────┐
         │   Layer 6: Audit Logging         │
         │   • Admin action tracking        │
         │   • Evidence preservation        │
         │   • Compliance support           │
         └──────────────────────────────────┘
```

---

## Component Interaction Matrix

```
┌──────────────────┬───────┬───────┬─────────┬──────────┬───────────┐
│ Component        │ Chats │ Msgs  │ Users   │ Claims   │ Items     │
├──────────────────┼───────┼───────┼─────────┼──────────┼───────────┤
│ ChatsListPage    │  R    │       │    R    │          │     R     │
├──────────────────┼───────┼───────┼─────────┼──────────┼───────────┤
│ ChatPage         │  R/U  │ R/W   │    R    │          │     R     │
├──────────────────┼───────┼───────┼─────────┼──────────┼───────────┤
│ AdminControls    │  U    │  U/D  │    R    │          │           │
├──────────────────┼───────┼───────┼─────────┼──────────┼───────────┤
│ ClaimApproval    │  W    │       │         │    U     │     R     │
├──────────────────┼───────┼───────┼─────────┼──────────┼───────────┤
│ Backend API      │ R/W/U │ R/W/D │    R    │    R     │     R     │
├──────────────────┼───────┼───────┼─────────┼──────────┼───────────┤
│ Database Triggers│  U    │       │         │          │           │
└──────────────────┴───────┴───────┴─────────┴──────────┴───────────┘

Legend:
R = Read
W = Write (Create)
U = Update
D = Delete
```

---

## State Diagram - Chat Status

```
           ┌─────────────────┐
           │  Claim Pending  │
           └────────┬────────┘
                    │
                    │ Claim Approved
                    ▼
           ┌─────────────────┐
      ┌────│ Chat Created     │────┐
      │    │ (enabled=true)   │    │
      │    └─────────┬────────┘    │
      │              │              │
      │              │              │ User closes
      │ Admin        │              │ (item returned)
      │ freezes      │              │
      │              │              ▼
      │              │     ┌─────────────────┐
      │              │     │  Chat Closed     │
      │              │     │ (is_closed=true) │
      │              │     └─────────────────┘
      │              │
      │              │ Normal
      │              │ messaging
      │              ▼
      │     ┌─────────────────┐
      └────▶│  Chat Active     │
            │ • Send messages  │
            │ • Real-time      │
            └────────┬─────────┘
                     │
                     │ Admin action
                     ▼
            ┌─────────────────┐
            │  Chat Frozen     │
            │ (is_frozen=true) │
            │ • No new messages│
            │ • View only      │
            └────────┬─────────┘
                     │
                     │ Admin
                     │ unfreezes
                     ▼
            ┌─────────────────┐
            │  Chat Active     │
            │ (back to normal) │
            └─────────────────┘
```

---

## Deployment Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         PRODUCTION                               │
│                                                                  │
│  ┌────────────────────────────────────────────────────────┐     │
│  │               CDN (Cloudflare/CloudFront)              │     │
│  │  • Static assets                                       │     │
│  │  • React frontend                                      │     │
│  │  • DDoS protection                                     │     │
│  └────────────────────┬───────────────────────────────────┘     │
│                       │                                          │
│                       ▼                                          │
│  ┌────────────────────────────────────────────────────────┐     │
│  │           Frontend (Vercel/Netlify)                    │     │
│  │  • React app                                           │     │
│  │  • Environment variables                               │     │
│  │  • Automatic deployments                               │     │
│  └────────────────────┬───────────────────────────────────┘     │
│                       │                                          │
│                       ├──────────────┬───────────────┐           │
│                       ▼              ▼               ▼           │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐          │
│  │              │  │   Backend    │  │   Supabase   │          │
│  │   Backend    │  │   Instance   │  │   (Database) │          │
│  │   (Primary)  │  │   (Replica)  │  │              │          │
│  │              │  │              │  │  • PostgreSQL│          │
│  │  • Node.js   │  │  • Node.js   │  │  • Auth API  │          │
│  │  • Express   │  │  • Express   │  │  • Realtime  │          │
│  │  • Rate      │  │  • Rate      │  │  • Storage   │          │
│  │    limiting  │  │    limiting  │  │              │          │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘          │
│         │                 │                 │                   │
│         └─────────────────┼─────────────────┘                   │
│                           │                                     │
│                           ▼                                     │
│  ┌────────────────────────────────────────────────────────┐     │
│  │              Redis (Rate Limiting)                     │     │
│  │  • Distributed rate limit tracking                     │     │
│  │  • Session storage                                     │     │
│  └────────────────────────────────────────────────────────┘     │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## Database Schema Relationships

```
┌─────────────────┐
│   user_profiles │
│  • user_id (PK) │
│  • full_name    │
│  • avatar_url   │
│  • role         │
│  • account_     │
│    status       │
└────────┬────────┘
         │
         │ 1:N
         ▼
    ┌────────────────────────────────┐
    │            chats                │
    │  • id (PK)                      │
    │  • item_id (FK) ────────────┐   │
    │  • claim_id (FK, UNIQUE) ───│┐  │
    │  • finder_id (FK) ───────┐  ││  │
    │  • claimant_id (FK) ─────│┐ ││  │
    │  • enabled              │││ ││  │
    │  • is_closed            │││ ││  │
    │  • is_frozen            │││ ││  │
    │  • frozen_by (FK) ──────│┼│││┤  │
    │  • freeze_reason        │││││┤  │
    │  • finder_unread_count  │││││┤  │
    │  • claimant_unread_count│││││┤  │
    │  • last_message_at      │││││┤  │
    └─────────┬────────────────┘││││┤  │
              │                 │││││  │
              │ 1:N             │││││  │
              ▼                 │││││  │
    ┌──────────────────┐        │││││  │
    │    messages      │        │││││  │
    │  • id (PK)       │        │││││  │
    │  • chat_id (FK) ─┘        │││││  │
    │  • sender_id (FK)─────────┘│││  │
    │  • message_text            ││┤  │
    │  • is_read                 ││┤  │
    │  • is_deleted              ││┤  │
    │  • created_at              ││┤  │
    └────────────────────────────┘│┤  │
                                  ││  │
                                  ││  │
    ┌─────────────────┐           ││  │
    │     items       │           ││  │
    │  • id (PK) ─────────────────┘│  │
    │  • title        │            │  │
    │  • finder_id    │            │  │
    └─────────────────┘            │  │
                                   │  │
    ┌─────────────────┐            │  │
    │     claims      │            │  │
    │  • id (PK) ────────────────────┘
    │  • item_id      │
    │  • claimant_id  │
    │  • status       │
    └─────────────────┘
```

---

## Performance Optimization Points

```
1. Database Indexes
   ├── chats(finder_id)
   ├── chats(claimant_id)
   ├── chats(claim_id) - UNIQUE
   ├── chats(is_frozen)
   ├── messages(chat_id, created_at)
   ├── messages(is_deleted)
   └── audit_logs(user_id, created_at)

2. Query Optimization
   ├── Use SELECT specific columns (not *)
   ├── Limit result sets (LIMIT 50)
   ├── Paginate with cursor-based pagination
   └── Cache frequently accessed data

3. Real-time Optimization
   ├── Targeted subscriptions (filter by chat_id)
   ├── Unsubscribe when leaving page
   ├── Debounce typing indicators
   └── Batch read receipts

4. Frontend Optimization
   ├── Virtual scrolling for long message lists
   ├── Lazy load older messages
   ├── Memoize expensive components
   └── Code splitting (React.lazy)

5. Backend Optimization
   ├── Use Redis for rate limiting
   ├── Connection pooling for database
   ├── Compress responses (gzip)
   └── Implement caching headers
```

---

## Monitoring & Alerting

```
┌─────────────────────────────────────────────────────────────┐
│                    Monitoring Stack                          │
│                                                              │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Application Metrics                     │   │
│  │  • Messages sent/minute                              │   │
│  │  • Chats created/day                                 │   │
│  │  • Active users                                      │   │
│  │  • Rate limit hits                                   │   │
│  │  • Error rates                                       │   │
│  └──────────────────────────────────────────────────────┘   │
│                           │                                  │
│                           ▼                                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Database Metrics                        │   │
│  │  • Query performance                                 │   │
│  │  • Connection count                                  │   │
│  │  • Table sizes                                       │   │
│  │  • Index usage                                       │   │
│  └──────────────────────────────────────────────────────┘   │
│                           │                                  │
│                           ▼                                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Security Metrics                        │   │
│  │  • Failed auth attempts                              │   │
│  │  • Admin actions (freeze/unfreeze)                   │   │
│  │  • Suspicious patterns                               │   │
│  │  • RLS policy violations                             │   │
│  └──────────────────────────────────────────────────────┘   │
│                           │                                  │
│                           ▼                                  │
│  ┌──────────────────────────────────────────────────────┐   │
│  │              Alerting Rules                          │   │
│  │  • Error rate > 5% → Page on-call                    │   │
│  │  • Response time > 2s → Warning                      │   │
│  │  • Rate limit hit > 100/min → Investigate            │   │
│  │  • Database CPU > 80% → Scale up                     │   │
│  └──────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────┘
```

---

## File Structure

```
project/
├── backend/
│   └── src/
│       ├── routes/
│       │   └── chatRoutes.ts ──────┐
│       ├── middleware/              │
│       │   ├── auth.ts              │
│       │   └── rateLimit.ts         │
│       └── index.ts ────────────────┤
│                                    │
├── frontend/                        │
│   └── src/                         │
│       ├── pages/                   │
│       │   ├── ChatPage.jsx ────────┤
│       │   ├── ChatsListPage.jsx ───┤
│       │   └── ClaimApprovalPage.jsx│
│       ├── components/              │
│       │   └── AdminChatControls.jsx│
│       └── lib/                     │
│           └── supabase.js          │
│                                    │
├── supabase/                        │
│   ├── schema.sql                   │
│   └── migrations/                  │
│       └── CHAT_SYSTEM_DATABASE_    │
│           SETUP.sql ───────────────┘
│
└── docs/
    ├── CHAT_SYSTEM_COMPLETE_
    │   DOCUMENTATION.md
    ├── CHAT_SYSTEM_QUICK_START.md
    ├── CHAT_SYSTEM_SECURITY_
    │   DECISIONS.md
    └── CHAT_SYSTEM_ARCHITECTURE.md (this file)
```

---

This architecture provides:
- ✅ Clear separation of concerns
- ✅ Defense in depth security
- ✅ Scalable design
- ✅ Real-time capabilities
- ✅ Comprehensive monitoring
- ✅ Production-ready implementation
